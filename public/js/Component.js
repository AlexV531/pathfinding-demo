export default class Component {
    
	constructor() {
		this.parent
	}

	// Don't use this method unless setting the component in the gameobject's constructor
	setParent(parent) {
		this.parent = parent
	}

	update(deltaTime) {

	}

	render(context) {

	}
}